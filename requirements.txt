strands-agents==1.55.0
# the AgentCore SDK, also used to test the entrypoint locally
bedrock-agentcore==1.23.0
# local model provider used when MODEL_PROVIDER=ollama
ollama==0.6.2
boto3==1.43.90
httpx==0.28.1
pypdf==6.18.0
# gradio 4.x is unusable on a current stack: it only pins fastapi<1.0, and starlette 1.x
# changed the TemplateResponse signature, so every page render returns a 500
gradio==5.50.0
# gradio still pulls pydub, which needs the audioop module dropped in Python 3.13
audioop-lts==0.2.2; python_version >= "3.13"
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp==1.44.0
python-dotenv==1.2.3
pyyaml==6.0.3
pytest==9.1.1
